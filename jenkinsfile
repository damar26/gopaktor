
#!/usr/bin/env groovy

node {
  stage 'Checkout'
  checkout scm

  ws (pwd() + '/docker-build-android') {
    stage 'Build Image'
        def img = docker.build "dr.gopaktor.com/paktor/build-android:${env.BUILD_NUMBER}"

    stage 'Publish'
  	    img.push()
  	    img.push('latest')
  }
}

#!/usr/bin/env groovy

node {
    stage 'Build and Deploy'
    docker.image('dr.gopaktor.com/paktor/build-android:latest').inside {
        checkout scm

        sh "ls -al"
        sh "pwd"
        sh "export ANDROID_HOME=“/opt/android-sdk-linux"
        sh "docker run dr.gopaktor.com/paktor/build-android:latest ls -al /opt/android-sdk-linux"
    }
}
